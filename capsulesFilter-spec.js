QUnit.module('capsulesFilter', function() {
  QUnit.test('filters capsules by `text` filter correctly', function(assert) {
    //code here
  });

  QUnit.test('filters capsules by `minIntensity` filter correctly', function(
    assert
  ) {
    //code here
  });

  QUnit.test('filters capsules by `orderAlpha` filter correctly', function(
    assert
  ) {
    //code here
  });

  QUnit.test(
    'filters capsules by `orderReverseAlpha` filter correctly',
    function(assert) {
      //code here
    }
  );

  QUnit.test('filters capsules by multiple filters correctly', function(
    assert
  ) {
    //code here
  });

  QUnit.test('does not mutate passed data', function(assert) {
    //code here
  });

  //other tests here
});
